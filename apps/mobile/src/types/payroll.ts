// PayrollRecord model interface
export interface PayrollRecord {
  id: string;
  userId: string;
  salary: number;
  periodStart: string;
  periodEnd: string;
  createdAt: string;
  updatedAt: string;
}
