# ================================
# 🗄️ DATABASE (Neon or Railway)
# ================================
# Main connection string with connection pooling
DATABASE_URL="postgresql://user:password@localhost:5432/vibewell?schema=public"
# Direct connection for migrations (required for Neon)
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"

# ================================
# ⚡ REDIS (Upstash or Render)
# ================================
REDIS_URL="redis://localhost:6379"
REDIS_TOKEN="your-redis-access-token" # For Upstash REST API (optional)

# ================================
# 🔐 AUTH0 (Authentication)
# ================================
AUTH0_SECRET="your-long-random-string"
AUTH0_BASE_URL="http://localhost:3000" # Use actual domain in production
AUTH0_ISSUER_BASE_URL="https://your-tenant.auth0.com"
AUTH0_CLIENT_ID="your-client-id"
AUTH0_CLIENT_SECRET="your-client-secret"

# ================================
# 💳 STRIPE (Payments & Subscriptions)
# ================================
STRIPE_SECRET_KEY="sk_test_..." # Use sk_live_... in production
STRIPE_PUBLISHABLE_KEY="pk_test_..." # Use pk_live_... in production
STRIPE_WEBHOOK_SECRET="whsec_..."
STRIPE_PRICE_ID="price_..." # For default subscription plan

# ================================
# ✉️ EMAIL (SendGrid / Resend / Postmark)
# ================================
EMAIL_PROVIDER_API_KEY="your-email-provider-api-key"
EMAIL_FROM="noreply@vibewell.com"
EMAIL_DOMAIN="vibewell.com"

# SMTP Configuration (if using SMTP)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your-smtp-user"
SMTP_PASS="your-smtp-password"

# ================================
# 📦 FILE STORAGE (Cloudinary or AWS S3)
# ================================
# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-cloudinary-api-key"
CLOUDINARY_API_SECRET="your-cloudinary-api-secret"

# AWS S3 Configuration (alternative to Cloudinary)
S3_BUCKET_NAME="vibewell-media"
S3_REGION="us-east-1"
S3_ACCESS_KEY_ID="your-access-key"
S3_SECRET_ACCESS_KEY="your-secret-key"

# ================================
# 📱 TWILIO (SMS Notifications)
# ================================
TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxx"
TWILIO_AUTH_TOKEN="your-twilio-token"
TWILIO_PHONE_NUMBER="+1234567890"

# ================================
# 🔔 FIREBASE (Push Notifications)
# ================================
FIREBASE_API_KEY="..."
FIREBASE_AUTH_DOMAIN="..."
FIREBASE_PROJECT_ID="..."
FIREBASE_MESSAGING_SENDER_ID="..."
FIREBASE_APP_ID="..."

# Push Notification Configuration
VAPID_PUBLIC_KEY="your-vapid-public-key"
VAPID_PRIVATE_KEY="your-vapid-private-key"
VAPID_SUBJECT="mailto:admin@vibewell.com"

# ================================
# 🐛 SENTRY (Error Monitoring)
# ================================
SENTRY_DSN="https://xyz@sentry.io/project-id"
SENTRY_ENVIRONMENT="development" # or production/staging

# ================================
# 📊 ANALYTICS (Optional)
# ================================
MIXPANEL_TOKEN="your-mixpanel-token"

# ================================
# 🧠 OPENAI (AI Features)
# ================================
OPENAI_API_KEY="sk-..."

# ================================
# ⚙️ APP CONFIG
# ================================
NODE_ENV="development" # or production/staging
PORT="3000"
JWT_SECRET="your-secure-jwt-secret"
JWT_EXPIRES_IN="7d"
ENCRYPTION_KEY="32-char-random-string"
COOKIE_SECRET="secure-random-cookie-secret"
NEXTAUTH_SECRET="use-a-secure-random-string-here"
NEXTAUTH_URL="http://localhost:3000"
ADMIN_EMAIL="founder@vibewell.com"

# ================================
# 🌐 VERCEL ENV FLAGS
# ================================
NEXT_PUBLIC_VERCEL_ENV="development" # or staging/production
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ================================
# 🔒 SECURITY
# ================================
# WebAuthn Configuration
WEBAUTHN_RP_ID="localhost"
WEBAUTHN_ORIGIN="http://localhost:3000"

# ================================
# 💡 IMPORTANT NOTES
# ================================
# 1. Never commit .env to Git - use this .env.example as a template
# 2. Use .env.local for local development
# 3. Set all production values in Vercel → Project Settings → Environment Variables
# 4. Use strong, unique values for all secrets
# 5. Rotate secrets regularly in production
# 6. Enable different features by adding/removing optional sections
# 7. Some services might require additional configuration in their respective dashboards

# API Keys (if needed)
OPENAI_API_KEY=your-openai-api-key

# AWS S3 (for file uploads)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=your-region
AWS_BUCKET_NAME=your-bucket-name

# Next.js
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NODE_ENV=development # change to 'production' in prod 