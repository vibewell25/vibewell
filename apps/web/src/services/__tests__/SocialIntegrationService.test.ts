/* eslint-disable */import * as mod from '../social-integration-service.test';

describe('social-integration-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
