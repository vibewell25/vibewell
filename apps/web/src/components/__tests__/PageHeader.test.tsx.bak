/* eslint-disable *//* eslint-disable *//* eslint-disable */import React from 'react';
import { render, screen } from '@testing-library/react';
import page-header from '../page-header.tsx';

describe('page-header', () => {
  it('renders without crashing', () => {
    const { container } = render(<page-header />);
    expect(container).toBeInTheDocument();
  });
});
