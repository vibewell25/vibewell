/* eslint-disable */import * as mod from '../recommendation-service.test';

describe('recommendation-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
