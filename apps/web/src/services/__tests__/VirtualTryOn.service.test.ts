/* eslint-disable */import * as mod from '../virtual-try-on.service.test';

describe('virtual-try-on.service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
