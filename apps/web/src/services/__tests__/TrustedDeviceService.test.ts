/* eslint-disable */import * as mod from '../trusted-device-service.test';

describe('trusted-device-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
