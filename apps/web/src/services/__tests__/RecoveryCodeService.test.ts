/* eslint-disable */import * as mod from '../recovery-code-service.test';

describe('recovery-code-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
