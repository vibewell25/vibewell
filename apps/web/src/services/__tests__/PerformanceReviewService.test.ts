/* eslint-disable */import * as mod from '../performance-review-service.test';

describe('performance-review-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
