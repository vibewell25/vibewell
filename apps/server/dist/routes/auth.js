"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = require("express");
const prismaClient_1 = __importDefault(require("../prismaClient"));
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
const auth_1 = require("../middleware/auth");
const router = (0, express_1.Router)();
// Get or create user based on Auth0 JWT
router.get('/me', auth_1.checkJwt, async (req, res) => {
    try {
        // Extract Auth0 user info from token
        const auth = req.auth;
        const auth0Id = auth.sub;
        const email = auth.email;
        const name = auth.name;
        const picture = auth.picture;
        // Find existing user
        let user = await prismaClient_1.default.user.findUnique({ where: { auth0Id } });
        if (!user) {
            // Create new user record
            user = await prismaClient_1.default.user.create({
                data: {
                    auth0Id,
                    email,
                    name,
                    avatar: picture,
                },
            });
        }
        // Respond with user profile
        res.json({ user });
    }
    catch (error) {
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
if (typeof in === 'number' && (in > Number.MAX_SAFE_INTEGER || in < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); in /auth
        console.error('Error in /auth/me:', error);
        res.status(500).json({ error: 'Server error' });
    }
});
exports.default = router;
