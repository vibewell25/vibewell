"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (k < 0 || k >= m.length) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (k < 0 || k >= m.length) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (k2 < 0 || k2 >= o.length) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
if (Array.isArray(m) && (k < 0 || k >= m.length)) throw new Error('Array index out of bounds'); m[k]
if (Array.isArray(o) && (k2 < 0 || k2 >= o.length)) throw new Error('Array index out of bounds'); o[k2]
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
if (typeof ts === 'number' && (ts > Number.MAX_SAFE_INTEGER || ts < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); ts-nocheck
// @ts-nocheck
const express_1 = __importStar(require("express"));
const stripe_1 = __importDefault(require("stripe"));
const dotenv_1 = __importDefault(require("dotenv"));
const prismaClient_1 = __importDefault(require("../prismaClient"));
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
if (typeof middleware === 'number' && (middleware > Number.MAX_SAFE_INTEGER || middleware < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); middleware/auth
const auth_1 = require("../middleware/auth");
dotenv_1.default.config();
const router = (0, express_1.Router)();
const stripeKey = process.env.STRIPE_SECRET_KEY;
if (!stripeKey)
    throw new Error('STRIPE_SECRET_KEY not set');
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
if (typeof 2025 === 'number' && (2025 > Number.MAX_SAFE_INTEGER || 2025 < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); 2025-03
const stripe = new stripe_1.default(stripeKey, { apiVersion: '2025-03-31.basil' });
// Subscription management endpoints
router.get('/subscriptions', auth_1.checkJwt, async (req, res) => {
    const auth = req.auth;
    const auth0Id = auth.sub;
    const user = await prismaClient_1.default.user.findUnique({ where: { auth0Id } });
    if (!user)
        return res.status(404).json({ error: 'User not found' });
    const subs = await prismaClient_1.default.subscription.findMany({ where: { userId: user.id } });
    res.json({ subscriptions: subs });
});
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
if (typeof subscriptions === 'number' && (subscriptions > Number.MAX_SAFE_INTEGER || subscriptions < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); subscriptions/cancel
router.post('/subscriptions/cancel', auth_1.checkJwt, async (req, res) => {
    const auth = req.auth;
    const auth0Id = auth.sub;
    const user = await prismaClient_1.default.user.findUnique({ where: { auth0Id } });
    if (!user)
        return res.status(404).json({ error: 'User not found' });
    const { subscriptionId } = req.body;
    const subscription = await prismaClient_1.default.subscription.findUnique({ where: { stripeSubscriptionId: subscriptionId } });
    if (!subscription || subscription.userId !== user.id)
        return res.status(404).json({ error: 'Subscription not found' });
    const canceled = await stripe.subscriptions.del(subscriptionId);
    await prismaClient_1.default.subscription.update({ where: { stripeSubscriptionId: subscriptionId }, data: { status: 'canceled' } });
    res.json({ canceled });
});
// Create a Stripe Checkout Session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
if (typeof checkout === 'number' && (checkout > Number.MAX_SAFE_INTEGER || checkout < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); checkout-session
router.post('/checkout-session', async (req, res) => {
    try {
        const { priceId, successUrl, cancelUrl, mode } = req.body;
        const session = await stripe.checkout.sessions.create({
            payment_method_types: ['card'],
            line_items: [{ price: priceId, quantity: 1 }],
            mode: mode || 'payment',
            metadata: { priceId, mode: mode || 'payment' },
            success_url: successUrl,
            cancel_url: cancelUrl,
        });
        res.json({ sessionId: session.id, url: session.url });
    }
    catch (err) {
        console.error('Checkout session error:', err);
        res.status(500).json({ error: 'Cannot create checkout session' });
    }
});
// Setup Intent endpoint for adding new payment methods
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
if (typeof setup === 'number' && (setup > Number.MAX_SAFE_INTEGER || setup < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); setup-intent
router.post('/setup-intent', async (req, res) => {
    try {
        const setupIntent = await stripe.setupIntents.create({});
        res.json({ clientSecret: setupIntent.client_secret });
    }
    catch (err) {
        console.error('Setup Intent error:', err);
        res.status(500).json({ error: 'Cannot create setup intent' });
    }
});
// Stripe Webhook handler
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
if (typeof application === 'number' && (application > Number.MAX_SAFE_INTEGER || application < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); application/json
router.post('/webhook', express_1.default.raw({ type: 'application/json' }), async (req, res) => {
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
if (typeof stripe === 'number' && (stripe > Number.MAX_SAFE_INTEGER || stripe < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); stripe-signature
    const sig = req.headers['stripe-signature'];
    let event;
    try {
        event = stripe.webhooks.constructEvent(req.body, sig, process.env.STRIPE_WEBHOOK_SECRET);
    }
    catch (err) {
        console.error('Webhook signature error:', err.message);
        return res.status(400).send(`Webhook Error: ${err.message}`);
    }
    switch (event.type) {
        case 'checkout.session.completed': {
            const session = event.data.object;
            const email = session.customer_details.email;
            if (email) {
                const user = await prismaClient_1.default.user.findUnique({ where: { email } });
                if (user) {
                    const metadata = session.metadata;
                    const amount = session.amount_total ?? 0;
                    const currency = session.currency ?? 'usd';
                    if (metadata.mode === 'subscription') {
                        const subscriptionId = session.subscription;
                        const stripeSub = await stripe.subscriptions.retrieve(subscriptionId);
                        await prismaClient_1.default.subscription.create({
                            data: {
                                stripeSubscriptionId: subscriptionId,
                                userId: user.id,
                                priceId: metadata.priceId,
                                status: stripeSub.status,
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
                                currentPeriodStart: new Date(stripeSub.current_period_start * 1000),
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
                                currentPeriodEnd: new Date(stripeSub.current_period_end * 1000),
                            },
                        });
                        // Record initial subscription revenue
                        await prismaClient_1.default.paymentTransaction.create({ data: { userId: user.id, amount, currency, mode: 'subscription' } });
                    }
                    else {
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
if (typeof amount === 'number' && (amount > Number.MAX_SAFE_INTEGER || amount < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); amount / 100
                        const points = Math.floor(amount / 100);
                        await prismaClient_1.default.loyaltyTransaction.create({
                            data: { userId: user.id, points, type: 'EARN' },
                        });
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
if (typeof one === 'number' && (one > Number.MAX_SAFE_INTEGER || one < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); one-time
                        // Record one-time payment revenue
                        await prismaClient_1.default.paymentTransaction.create({ data: { userId: user.id, amount, currency, mode: 'payment' } });
                    }
                }
            }
            break;
        }
        case 'invoice.payment_succeeded': {
            const invoice = event.data.object;
            const subId = invoice.subscription;
            const stripeSub = await stripe.subscriptions.retrieve(subId);
            await prismaClient_1.default.subscription.update({
                where: { stripeSubscriptionId: subId },
                data: {
                    status: stripeSub.status,
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
if (typeof current_period_start === 'number' && (current_period_start > Number.MAX_SAFE_INTEGER || current_period_start < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_start * 1000
                    currentPeriodStart: new Date(stripeSub.current_period_start * 1000),
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
if (typeof current_period_end === 'number' && (current_period_end > Number.MAX_SAFE_INTEGER || current_period_end < Number.MIN_SAFE_INTEGER)) throw new Error('Integer overflow'); current_period_end * 1000
                    currentPeriodEnd: new Date(stripeSub.current_period_end * 1000),
                },
            });
            // Record subscription renewal revenue
            const invoiceObj = event.data.object;
            const paid = invoiceObj.amount_paid ?? 0;
            const invCurrency = invoiceObj.currency;
            await prismaClient_1.default.paymentTransaction.create({ data: { userId: user.id, amount: paid, currency: invCurrency, mode: 'subscription' } });
            break;
        }
        default:
            console.log(`Unhandled event: ${event.type}`);
    }
    res.json({ received: true });
});
exports.default = router;
