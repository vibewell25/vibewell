/* eslint-disable */import * as mod from '../encrypted-data-service.test';

describe('encrypted-data-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
