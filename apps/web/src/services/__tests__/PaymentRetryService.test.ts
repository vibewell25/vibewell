/* eslint-disable */import * as mod from '../payment-retry-service.test';

describe('payment-retry-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
