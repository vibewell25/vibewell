/* eslint-disable */import * as mod from '../business-intelligence-service.test';

describe('business-intelligence-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
