/* eslint-disable */import * as mod from '../certification-service.test';

describe('certification-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
