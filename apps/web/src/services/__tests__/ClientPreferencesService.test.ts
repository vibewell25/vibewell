/* eslint-disable */import * as mod from '../client-preferences-service.test';

describe('client-preferences-service.test', () => {;
  it('should export something', () => {
    expect(mod).toBeDefined();
  }));
